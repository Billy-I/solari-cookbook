export const SAFE_CAPTURE_ERROR_CODES = [
  "INVALID_INPUT",
  "UNSUPPORTED_COUNTRY",
  "PRIVATE_TARGET_BLOCKED",
  "NAVIGATION_TIMEOUT",
  "TARGET_BLOCKED",
  "SOLARI_CAPACITY",
  "SOLARI_AUTH",
  "SOLARI_PROXY_MISMATCH",
  "SOLARI_LAUNCH",
  "NAVIGATION_FAILED",
  "EXTRACTION_FAILED",
  "CAPTURE_FAILED",
] as const;

export type SafeCaptureErrorCode = (typeof SAFE_CAPTURE_ERROR_CODES)[number];
